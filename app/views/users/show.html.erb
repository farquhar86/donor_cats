<%= render "layouts/navbar" %>
<div class="container">
	<div class="row">
		<div class="jumbotron" id="profile_jumbo">
			<div class="row">
				<div class="col-lg-3 text-center">
					<%= image_tag("profile.jpg", alt: "Cool Kangaroo", class: "profile_pic") %>
				</div>
				<div class="col-lg-6">
					<h2><%= @user.full_name %></h2>
					<p>Member since <%= @user.created_at.strftime("%D") %></p>
					<br>
					<p>Currently donating <span class="green">$<%= @user.donations.inject(0) {|sum, d| sum + d.monthly_amount} %></span> per month</p>
					<br>

					<% @user.donations.each do |d| %>
						<p>Next donation: $<%= d.monthly_amount %> on <%= (d.created_at + 1.month).strftime("%D") %></p>
					<% end %>
				</div>
				<div class="col-lg-3 text-center">
					<h1 class="green">$<%= @user.donations.inject(0) {|sum, d| sum + d.amount} + @user.donations.inject(0) {|sum, d| sum + d.monthly_amount} %></h1>
						<!-- Needs some kind of * (Time.now - @user.donations.created_at.to_time)/1.month).round -->
					<h4>Donated so far!</h4>
				</div>
			</div>
		</div>
	</div>
	<hr>
	<div class="row">
		<div class="text-center">
			<h3>Your selected charities</h3>
		</div>
		<ul id="profile_charities">
			<% @user.donations.each do |c| %>
				<li>Charity ID: <%= c.charity_id %> [name coming]</li>
			<% end %>
		</ul>
	</div>
	<hr>
	<div class="row text-center">
		<a class="btn btn-success" href="/charities">Find More Charities!</a>
	</div>
	<hr>
	<div class="row">
		<div class="col-lg-6">
			<h2 class="profile_title">News Feed</h2>
			<% @user.donations.each do |d| %>
			<div class="panel panel-primary">
				<div class="panel-heading">[Charity Name/Picture]</div>
				<div class="panel-body">
					<p>Meditation hoodie freegan, shoreditch pork belly listicle fanny pack pitchfork truffaut. Meggings shabby chic jean shorts, keffiyeh vinyl neutra cold-pressed blog. Gentrify cred sriracha vegan, banh mi chillwave single-origin coffee heirloom salvia pug godard roof party forage.<br><br>Synth tofu craft beer, gentrify gluten-free direct trade helvetica. Locavore mustache PBR&B lo-fi photo booth, health goth venmo. Locavore quinoa tofu, typewriter trust fund DIY YOLO. Drinking vinegar literally disrupt, lomo lumbersexual craft beer lo-fi meh heirloom pickled tousled vegan etsy distillery retro.</p>
				</div>
			</div>
			<% end %>
		</div>
		<div class="col-lg-6">
			<h2 class="profile_title">Your Donations</h2>
			<% @user.donations.reverse.each do |d| %>
				<div class="panel panel-success">
					<div class="panel-heading">[Charity Name/Picture]</div>
					<div class="panel-body">
						<p><%= d.created_at.strftime("%D") %> at <%= d.created_at.strftime("%H:%m") %>, you donated:</p>
						<ul>
							<li>$<%= d.monthly_amount %> per month</li>
							<li>$<%= d.amount %> one-time</li>
						</ul>
					</div>
				</div>
			<% end %>
		</div>
	</div>
</div>